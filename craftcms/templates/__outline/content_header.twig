
	{# {% set menu = craft.globals().getSetByHandle('primaryMenu') %}
	{% set items = menu.menuItems.type('menuItem') %} #}

	<body>

		<div class="ui-viewport" id="main">

			{% if currentUser %}

			<div class="ui-view main" id="app">

			{% endif %}
